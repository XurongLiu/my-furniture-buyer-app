node_modules
.next
.env
.env.local
prisma/dev.db
prisma/dev.db-journal

# Large source input for scripts/extract-catalogue-pdf.mjs — the derived
# data/catalogue-parsed.json and data/catalogue-embeddings.json are what
# the app actually uses, and those stay committed.
data/source/
